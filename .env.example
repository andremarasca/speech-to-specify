# Auto-generated by scripts/validate_env.py
# Do NOT edit manually — regenerate with: python scripts/validate_env.py

# ── TelegramConfig ──
BOT_TOKEN=  # default: (computed)
ALLOWED_CHAT_ID=  # default: (computed)
DOWNLOAD_TIMEOUT=  # default: (computed)

# ── WhisperConfig ──
MODEL_NAME=  # default: (computed)
DEVICE=  # default: (computed)
FP16=  # default: (computed)
CACHE_DIR=  # default: (computed)
LANGUAGE=  # default: (computed)

# ── SessionConfig ──
SESSIONS_DIR=  # default: (computed)

# ── SearchConfig ──
MIN_SIMILARITY_SCORE=  # default: (computed)
MAX_RESULTS=  # default: (computed)
QUERY_TIMEOUT_SECONDS=  # default: (computed)
SEARCH_TIMEOUT_SECONDS=  # default: (computed)
PAGE_SIZE=  # default: (computed)
HELP_FALLBACK_ENABLED=  # default: (computed)
ORPHAN_RECOVERY_PROMPT=  # default: (computed)

# ── Settings ──
LLM_PROVIDER=  # default: (computed)
OPENAI_API_KEY=  # default: (computed)
ANTHROPIC_API_KEY=  # default: (computed)
DEEPSEEK_API_KEY=  # default: (computed)
OUTPUT_DIR=  # default: (computed)
VERBOSE=  # default: (computed)
TIMEOUT=  # default: (computed)

# ── OracleConfig ──
ORACLES_DIR=  # default: (computed)
ORACLE_PLACEHOLDER=  # default: (computed)
ORACLE_CACHE_TTL=  # default: (computed)
LLM_TIMEOUT_SECONDS=  # default: (computed)
ORACLE_PROVIDER=  # default: (computed)
ORACLE_MODEL=  # default: (computed)

# ── UIConfig ──
MESSAGE_LIMIT=  # default: (computed)
PROGRESS_INTERVAL_SECONDS=  # default: (computed)
OPERATION_TIMEOUT_SECONDS=  # default: (computed)
AUDIO_QUEUE_MAX_SIZE=  # default: (computed)
CONFIRMATION_TIMEOUT_SECONDS=  # default: (computed)

# ── TTSConfig ──
ENABLED=  # default: (computed)
VOICE=  # default: (computed)
FORMAT=  # default: (computed)
TIMEOUT_SECONDS=  # default: (computed)
MAX_TEXT_LENGTH=  # default: (computed)
GC_RETENTION_HOURS=  # default: (computed)
GC_MAX_STORAGE_MB=  # default: (computed)
